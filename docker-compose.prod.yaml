services:
  api:
    container_name: erp_grupodu_v0.1.1
    build:
      context: .
      dockerfile: Dockerfile
      target: prod
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - DEV_PORT=8001
    ports:
      - "8001:8001"
    expose:
      - "8001"